name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
  
    - name: publish to artifactory
      uses: advancedcsg-open/action-jfrog-cli@master
      with:
        url: 'https://registry-1.docker.io/'
        credentials type: 'apikey'
        apikey: AKCp8nyYHF2oGeYep1SAYLYAaJHFNp8SLgZrL1ghD53c26nDG5Enb7UC1PqVHCB4fXz3FuNZd
        
    - name: Build the Docker image
      run: docker build . -t useast.jfrog.lexisnexisrisk.com/ui/native/innuk-dotnetdocker:${{ github.sha }}
   
    - name: Login to JFrog
      uses: docker/docker-login@v1
      with:
          login-server: useast.jfrog.lexisnexisrisk.com
          username: usernamedocker
          password: JE2f1dfg64YcH9L8mmfXF1kD7So9fomikFrcHQw4LnNCn1AxGU
    - name: Push the Docker image to the registry
      run: docker push useast.jfrog.lexisnexisrisk.com/ui/native/innuk-dotnetdocker:${{ github.sha }}
